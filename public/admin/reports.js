// This file is loaded on the reports page.
// Currently, all report downloads are handled by simple links,
// so no specific JavaScript is needed here.
// This file ensures that main.js (auth, navbar) is loaded and
// provides a place for future JS-driven reporting features.

document.addEventListener('DOMContentLoaded', () => {
    console.log('Reports page loaded.');
});
